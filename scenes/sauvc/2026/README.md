Профиль `sauvc/2026` создан как шаблон под новую сцену.

Что нужно добавить перед запуском:

- положить рядом с [config.yaml](config.yaml) реальный файл `sauvc_2026.blend`
- заменить placeholder-имена объектов, коллекций, материалов и labels на фактические значения из сцены
- при необходимости описать mist-модель в `scene.mist` и переключить `visibility.mode` на `mist_model`
